
            <p> WELCOME TO NELISA'S SPAZA SHOP</p>
            <h1>Welcome!</h1>

            {{#if user}}
                Hello, {{user.name}}
            {{/if}}

            <h2>You should only see this page if you are logged in!</h2>
            {{user.is_admin}}

            {{# if user.is_admin}}
            <h3>And this message if you are an admin user</h3>

            Welcome admin user!

            {{/if}}
            <h3>You should be able to logout of the app</h3>
            
