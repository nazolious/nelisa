<h1>PRODUCTS</h1>
{{# if user.is_admin}}
    <button type="button" class="btn btn-warning"><a href="/products/add">ADD</a></button>
    {{else}}
        <a></a>
        {{/if}}
        <div class="container">

            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>PRODUCT</th>
                        <th>CATEGORY</th>
                        {{# if user.is_admin}}
                            <th>EDIT</th>
                            <th>DELETE</th>
                            {{else}}
                                <td></td>
                                {{/if}}
                    </tr>
                </thead>
                <tbody>
                    {{#products}}
                        <tr>
                            <td>{{product}}</td>
                            <td>{{category}}</td>
                            {{# if ../is_admin}}
                                <td><button type="button" class="btn btn-warning"><a href="/products/edit/{{product_id}}">EDIT</a></button></a></td>
                                <td><button type="button" class="btn btn-danger"><a href="/products/delete/{{product_id}}">DELETE</a></button></a></td>
                                {{else}}
                                    <td></td>
                                    {{/if}}
                                  </tr>

                    {{/products}}
                </tbody>
            </table>
        </div>
